int main()
{
decl
    int temp;
enddecl
begin
    temp = exposcall("Fork");

    if(temp != 0) then
        //parent
        temp = exposcall("Exec", "even.xsm");
    else
        // child
        temp = exposcall("Exec", "oddnum.xsm");
    endif;

    return 0;
end
}