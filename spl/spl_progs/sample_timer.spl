//user stackptr at 13 th word  backup current proceess in kerenl
[PROCESS_TABLE + ( [SYSTEM_STATUS_TABLE + 1] * 16) + 13] = SP;

//setting sp to userpgnum*512-1
SP = [PROCESS_TABLE + ([SYSTEM_STATUS_TABLE + 1] * 16) + 11] * 512 - 1;

//bring the user contents to kernel using backup
backup;

//make current process ready
alias currentPID R0;
currentPID = [SYSTEM_STATUS_TABLE+1];

alias process_table_entry R1;
process_table_entry = PROCESS_TABLE + (currentPID * 16);
[process_table_entry + 4] = READY;
breakpoint;
//incrementing the timer tick
alias counter R3;
counter=0;
while(counter < 16)do
	[PROCESS_TABLE+counter*16]=[PROCESS_TABLE+counter*16] + 1;
	counter=counter+1;
endwhile;
call MOD_5;
breakpoint;

//restore user context from kernel stack
restore;

//getback the sp
SP = [PROCESS_TABLE + ([SYSTEM_STATUS_TABLE + 1] * 16) + 13];
//setting mode flag to zero
[PROCESS_TABLE+([SYSTEM_STATUS_TABLE+1]*16)+9]=0;

ireturn;
